const test=require("node:test"),assert=require("node:assert/strict"),fs=require("node:fs");
const web=fs.readFileSync(require.resolve("../webApp"),"utf8"),generator=fs.readFileSync(require.resolve("../services/strategyGenerator"),"utf8"),migration=fs.readFileSync(require.resolve("../migrations/20260820_love_type_profiles.sql"),"utf8");
test("love type profile is user-owned and can be overwritten",()=>{assert.match(web,/app\.get\("\/api\/v1\/love-type-profile",requireUser/);assert.match(web,/app\.put\("\/api\/v1\/love-type-profile",requireUser/);assert.match(web,/onConflict:"user_id"/);assert.match(migration,/user_id uuid primary key/);assert.match(migration,/enable row level security/)});
test("saved continuous scores personalize the shared strategy generator",()=>{assert.match(web,/from\("love_type_profiles"\).*maybeSingle/);assert.match(generator,/userLoveProfile/);assert.match(generator,/Use behaviorScores as scenario-derived preferences/);assert.match(generator,/current facts and observed signals take priority/)});
test("runtime metadata makes borderline and sparse behaviors low-confidence",()=>{assert.match(web,/LOVE_TYPE_BEHAVIOR_MAP/);assert.match(web,/withLoveTypeRuntimeMeta/);assert.match(web,/borderline/);assert.match(web,/effectiveQuestions/);assert.match(generator,/Read axisMeta before using axisScores/);assert.match(generator,/low-confidence auxiliary signals/);for(const weak of ["sn2","sn4","tf3","tf4"])assert.doesNotMatch(web,new RegExp(`${weak}\\\",\\\"[AB]\\\"\\]`))});
